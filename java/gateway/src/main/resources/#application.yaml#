server:
  port: 8000

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: site_local
          uri: forward:/index.html
          predicates:
            - Path=/
          


